@model int

<a href="/Notifications/Index" class="notification-btn-elegant">
    <i class="bi bi-bell"></i>
    @if (Model > 0)
    {
        <span class="notification-badge-elegant">@(Model > 99 ? "99+" : Model.ToString())</span>
    }
</a>
